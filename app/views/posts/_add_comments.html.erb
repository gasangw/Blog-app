<div>
<%= form_with url:"/users/#{@post.user_id}/posts/#{@post.id}/comments", scope: :comment do |f| %>
     <%= f.text_area :text, rows: 10, required: true %>
     <%= f.submit "Comment" %>
<%end%>
</div>